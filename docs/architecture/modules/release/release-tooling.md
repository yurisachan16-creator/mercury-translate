# Release Tooling

Release tooling builds and verifies local artifacts for GitHub Release and the
manual Chrome Web Store upload path. It must not submit to browser stores,
accept declarations, create service-account credentials, or automate payment.
